import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CFGI APP Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
